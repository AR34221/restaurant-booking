<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Бронирование — FLAVOR</title>
  <link
  href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600&display=swap" rel="stylesheet"/>

  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/booking.css" />
</head>
<body>

  <%- include('../partials/header', { page: 'booking', user: user }) %>

  <main>
    <!-- Выбор даты/времени -->
    <section class="booking-controls">
      <h2>Забронировать</h2>
      <select id="date"></select>
      <select id="time"></select>
    </section>

    <!-- Схема ресторана с гридом -->
    <section class="hall-wrapper">
      <div class="hall-labels">
        <div class="label left-label">Основной зал</div>
        <div class="label right-label">Веранда</div>
      </div>
      <div class="hall-map" style="background-image: url('/img/Restaurant_Layout.png');">
        <div class="hall-grid" data-location="Основной зал"></div>
        <div class="hall-grid" data-location="Веранда"></div>
      </div>
    </section>
  </main>

  <!-- Модальное окно для подтверждения бронирования -->
<div id="bookingModal" class="modal">
  <div class="modal-content">
    <!-- Кнопка закрыть -->
    <span class="close">&times;</span>

    <!-- Заголовок -->
    <div class="modal-title">
      <h2>Подтверждение бронирования</h2>
    </div>

    <!-- Фото + оверлей с номером стола и местами -->
    <div class="photo-wrapper">
      <img
        class="table-photo"
        id="tableImage"
        src="/img/Table_2.png"
        alt="Стол"
      />
      <span class="table-number" id="tableNumber">Стол 1</span>
      <span class="table-seats" id="tableSeats">2 места</span>
    </div>

    <!-- Дата и время -->
    <div class="date-time-wrapper">
      <div class="date-block">
        <span class="dt-label">Дата:</span>
        <span class="dt-value" id="selectedDate">2025-05-05</span>
      </div>
      <div class="time-block">
        <span class="dt-label">Время:</span>
        <span class="dt-value" id="selectedTime">10:00 – 11:30</span>
      </div>
    </div>

    <!-- Кнопка подтвердить -->
    <button id="bookButton">Забронировать</button>

    <!-- Сообщение об успехе -->
    <p id="successMessage">
      <span class="success-main">Успешно забронировано!</span><br>
      <span class="success-sub">Отменить бронь можно в личном кабинете</span>
    </p>
    
  </div>
</div>

  <%- include('../partials/footer') %>

  <!-- Подключаем скрипт после всей разметки -->
  <script src="/js/booking.js"></script>
</body>
</html>
